const  { getUserById } = require('./js-foundation/05-factory')

// const id = 2;

// getUserById(id, (error, user) => {
//     if ( error ) {
//         throw new Error(error)
//     }

//     console.log(user);
// })